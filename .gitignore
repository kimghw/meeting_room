node_modules/

# install.ps1 이 PC 마다 새로 만든다 — 그 PC 의 절대경로와 확장 ID 가 박혀 있다.
native/com.krs.meetingroom.json

# 개인 설정
.claude/settings.local.json

# 다리 호출 기록 — 입력한 문장·회의 주제·이름이 들어 있다. 공개 저장소에 올리지 않는다.
native/logs/
